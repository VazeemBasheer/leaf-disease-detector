# Path to the trained PyTorch model checkpoint (.pth file)
MODEL_PATH=models/resnet18_leaf_best.pth

# Softmax probability threshold for classifying a leaf sample as diseased (healthy vs diseased collapse)
DISEASE_THRESHOLD=0.35
